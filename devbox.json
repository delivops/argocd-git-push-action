{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.13.7/.schema/devbox.schema.json",
  "packages": ["nodejs@latest", "yarn@latest"],
  "shell": {
    "init_hook": ["yarn install"],
    "scripts": {
      "test": ["yarn ci-test"],
      "bundle": ["yarn bundle"]
    }
  }
}
